# D-ZINE 3.0 Website

This file is generated from .dzine.html and is not the source of truth.

## Objective

Launch a multi-page open-source product website generated from D-ZINE source files.

## Design DNA

- Visual dialect: precision-product
- Density: 6
- Variance: 8
- Motion: 3

## Recipes

- **web-landing** (web) — hero, proof, workflow, docs, quality
- **mobile-app-flow** (mobile) — hero, proof, docs, cta
- **brand-kit** (brand-kit) — palette, type, applications

## Anti-Slop Rules

- ai-purple-gradient: Replace the pattern with a role-based, context-specific design decision that can scale across states and surfaces.
- neon-glow-button: Replace the pattern with a role-based, context-specific design decision that can scale across states and surfaces.
- centered-hero-repetition: Replace the pattern with a role-based, context-specific design decision that can scale across states and surfaces.
- three-equal-card-row: Replace the pattern with a role-based, context-specific design decision that can scale across states and surfaces.
- nested-card-prison: Replace the pattern with a role-based, context-specific design decision that can scale across states and surfaces.
- meaningless-pill-clutter: Replace the pattern with a role-based, context-specific design decision that can scale across states and surfaces.
- fake-perfect-metric: Replace the pattern with a role-based, context-specific design decision that can scale across states and surfaces.
- generic-saas-copy: Replace the pattern with a role-based, context-specific design decision that can scale across states and surfaces.
- placeholder-brand: Replace the pattern with a role-based, context-specific design decision that can scale across states and surfaces.
- unreadable-tiny-text: Replace the pattern with a role-based, context-specific design decision that can scale across states and surfaces.
- missing-ui-states: Replace the pattern with a role-based, context-specific design decision that can scale across states and surfaces.
- inaccessible-motion: Replace the pattern with a role-based, context-specific design decision that can scale across states and surfaces.

## Source Rule

Use the .dzine.html document for visual guidance, JSON metadata, tokens, recipes, component specimens, and QA gates.
